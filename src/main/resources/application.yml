spring:
  application:
    name: blog
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3308/blog_platform?useTimezone=true&serverTimezone=UTC
    username: docker
    password: admin123
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
    show-sql: true
    open-in-view: false
jwt:
  secret: vou-trocar-depois-quando-for-subir-a-aplicacao-xd
spring-doc:
  paths-to-match: /api/v1/**
  swagger-ui:
    use-root-path: true